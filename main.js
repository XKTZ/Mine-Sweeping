$(function () {
    const backArr = []
    // backboard -> the board of mines
    let backBoard = [];
    // frontboard -> the board show to users
    let frontBoard = [];
    let win = false;
    function initBoard() {
        for(let i = 0; i < 11; i ++) {
            
        }
    }
});